select:
  type: select
  choices:
    - stuff
    - things
  message: |
    This is an example of a list hook. It takes in a list and returns on item.
    
    Basic example:
      type: input
      choices:
        - stuff
        - things
      message: Some message...
    
    Returns string
